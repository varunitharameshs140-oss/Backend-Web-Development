'use strict';
// data.js — a tiny user store so /auth/login has something to check.
// Given — do not modify. (Plaintext password is only for this exercise.)

const users = [
  { id: 'u-1', email: 'alice@example.com', password: 'correct-horse' },
];

module.exports = { users };
